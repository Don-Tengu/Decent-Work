# 开发 — Vite mode `sepolia` (`npm run dev:sepolia` / `npm run build:sepolia`)
# Chain: Base Sepolia. Token: Circle USDC from tokens.js.
# Set VITE_ESCROW_ADDRESS in `.env.sepolia.local` after deploying to Sepolia.

VITE_API_BASE_URL=http://localhost:8080
VITE_CHAIN_KEY=base-sepolia
VITE_ESCROW_ADDRESS=
